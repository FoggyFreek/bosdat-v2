# Database Connection
# For local development with Docker, use: Host=postgres;Database=bosdat;Username=bosdat;Password=YOUR_PASSWORD
# For local development without Docker, use: Host=localhost;Database=bosdat;Username=bosdat;Password=YOUR_PASSWORD
DB_CONNECTION_STRING=Host=postgres;Database=bosdat;Username=bosdat;Password=bosdat_dev_password

# JWT Secret (minimum 32 characters for HS256)
JWT_SECRET=YourSuperSecretKeyThatIsAtLeast32CharactersLong!

# Worker credentials (used by BosDAT.Worker to authenticate against the API)
WORKER_EMAIL=worker@bosdat.nl
WORKER_PASSWORD=YourWorkerPassword

# ─── Email (Brevo) ────────────────────────────────────────────────────────────
# Provider: "Brevo" to send real emails, "Console" to log to stdout (default for dev)
EMAIL_PROVIDER=Console
EMAIL_FROM_ADDRESS=noreply@bosdat.nl
EMAIL_FROM_NAME=BosDAT

# Brevo transactional email API key
# Get yours at: https://app.brevo.com/settings/keys/api
# Required when EMAIL_PROVIDER=Brevo
BREVO_API_KEY=your-brevo-api-key-here
